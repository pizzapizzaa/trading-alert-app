# Copy this file to .env and fill in your values before starting the admin server.

# Required: secret token the admin panel must send as "Authorization: Bearer <token>"
# Generate with: node -e "console.log(require('crypto').randomBytes(32).toString('hex'))"
ADMIN_API_SECRET=change-me-to-a-long-random-secret

# Optional: restrict CORS to your admin panel domain (defaults to localhost:3001)
# CORS_ORIGIN=https://your-admin-panel.vercel.app

# Optional: override port (default: 3001)
# PORT=3001
